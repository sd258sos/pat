#include <iostream>
#include <algorithm>
#include <vector>
using namespace std;
/*
输入 N个数字
输入 N个不同的分数
 

*/
int main(){
	int n1,n2;
	scanf("%d",&n1);
	vector <int> v(101);
	int b,c;
	for(int i=0;i<n1;i++){
		scanf("%d",&b);
		v[b]++;
	}
	scanf("%d",&n2);
	for(int j=0;j<n2;j++){
		scanf("%d",&c);
		if(j!=0){
			printf(" ");
		}
		printf("%d",v[c]);
	}
	 
	return 0;
}
